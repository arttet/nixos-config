# Commands

Commands are grouped through `just` modules.

Core checks:

```sh
just check
```

Guest VM:

```sh
just guest build
just guest run
just guest daemon
just guest ssh
just guest status
just guest stop
just guest clean
just guest test
```

Documentation:

```sh
just docs dev
just docs build
just docs preview
```
