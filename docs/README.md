# NixOS Platform docs

This directory contains the VitePress documentation portal for the NixOS
Platform.

## Commands

```sh
bun install
bun run docs:dev
bun run docs:build
bun run docs:preview
```

From the repository root, use the just module:

```sh
just docs dev
just docs build
just docs preview
```
