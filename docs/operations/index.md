# Operations

Operations docs describe the routine work around the platform: rebuilding,
cleanup, debugging, recovery, rollback, and backups.

Start with [Rebuilding](rebuilding.md). For failed workstation updates, see
[Rollback](rollback.md). For backup policy, see [Backups](backups.md).
