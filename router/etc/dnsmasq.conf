# /etc/dnsmasq.conf
no-resolv
server=127.0.0.1#5300     # forward to xray DNS inbound → DoH via tunnel

# Intercept all DNS queries arriving on LAN, regardless of destination
# (iptables redirect port 53 → dnsmasq)
